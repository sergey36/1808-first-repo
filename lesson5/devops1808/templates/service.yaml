apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops1808.fullname" . }}
  labels:
    {{- include "devops1808.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops1808.selectorLabels" . | nindent 4 }}
